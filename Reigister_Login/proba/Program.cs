using System;

namespace proba
{
    class Program
    {
        static void Main(string[] args)
        {
            char[] a = new char[] { 'a', 'h', 'm', 'D', 'l' };
            //int b = Char.
            for(int i = 0;i < a.Length; i++)
            {
                Console.WriteLine(a.Length -1);
            }

            Console.ReadLine();
        }
    }
}
